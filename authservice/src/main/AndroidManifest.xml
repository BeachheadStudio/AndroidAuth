<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.singlemalt.googleplay.auth.googleplayauth">

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Transparent">

        <activity android:name="com.singlemalt.googleplay.auth.googleplayauth.AuthServiceActivity"
            android:label="@string/app_name" android:theme="@style/Theme.Transparent"  />

        <meta-data android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <meta-data android:name="com.google.android.gms.games.APP_ID" android:value="\ YOUR_APP_ID"/>
    </application>
</manifest>
